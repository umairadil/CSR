# Copy to .env for local development. Do NOT commit your .env file.

# Example connection string for Prisma to a local/remote SQL Server
# IMPORTANT: Do not commit real credentials to git. Use this only for local dev.
DATABASE_URL="sqlserver://sa:INse14ron.@DESKTOP-4A1H4P7:1433;database=darjaah_csr;trustServerCertificate=true;encrypt=false"

# NextAuth secret for JWT encryption (generate your own strong secret)
NEXTAUTH_SECRET="replace-with-strong-secret"

# Optional: public app URL for auth callbacks when deploying
# NEXTAUTH_URL="http://localhost:3000"












